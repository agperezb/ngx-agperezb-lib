import { NgxFieldErrorDirective } from './ngx-field-error.directive';

describe('NgxFieldErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxFieldErrorDirective();
    expect(directive).toBeTruthy();
  });
});
